services:
  - type: web
    name: the-plug-swizzler
    env: python
    buildCommand: pip install -r requirements_minimal.txt
    startCommand: python app.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    plan: free